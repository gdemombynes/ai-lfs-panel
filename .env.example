# Copy to .env (git-ignored). Secrets are read by lfspanel.config.
MOSPI_API_KEY=            # microdata.gov.in > My Profile > API Key (India PLFS)
DATAFIRST_USER=           # DataFirst account (South Africa QLFS), optional
DATAFIRST_PASS=
# LFSPANEL_DATA_DIR=/Volumes/external/ai-lfs-panel-data   # move data/ off the repo disk
